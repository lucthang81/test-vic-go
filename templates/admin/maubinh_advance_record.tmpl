<html>
	<head>
		{{template "header_link_script" .}}
		{{.script_type}}
		{{.script_white_win}}
	</head>
	<body style="margin: 20px;">
		{{template "nav" .}}
		{{.form_type}}
		<hr/>
		{{.form_white_win}}


		Total: {{.total}} <br/>
		{{.result_table}}
	</body>
</html>